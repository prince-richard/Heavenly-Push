/**
 * Re-export the SpeechRecognitionAdapter interface from types.
 * This is the contract other modules consume for speech recognition.
 */
export type { SpeechRecognitionAdapter } from '@/types/speech';
